# Shop 工具合集 - 打包说明

## 快速打包（推荐）

### 方法一：使用自动打包脚本（最简单）

1. **安装依赖**（如果还没安装）：
   ```bash
   pip install -r requirements.txt
   ```

2. **运行打包脚本**：
   ```bash
   python build_exe.py
   ```

3. **打包完成后**：
   - exe 文件在 `dist/Shop工具合集.exe`
   - 可以直接运行或分发给其他用户

### 方法二：使用高级配置文件

1. **安装 PyInstaller**：
   ```bash
   pip install pyinstaller
   ```

2. **使用 spec 文件打包**：
   ```bash
   pyinstaller build_exe_advanced.spec
   ```

3. **打包完成后**：
   - exe 文件在 `dist/Shop工具合集.exe`

### 方法三：手动打包

1. **安装 PyInstaller**：
   ```bash
   pip install pyinstaller
   ```

2. **执行打包命令**：
   ```bash
   pyinstaller --name=Shop工具合集 --onefile --windowed --noconfirm shop_tools_gui.py
   ```

## 打包选项说明

- `--onefile`: 打包成单个 exe 文件（推荐，方便分发）
- `--windowed`: 不显示控制台窗口（GUI应用）
- `--name=Shop工具合集`: 指定输出文件名
- `--noconfirm`: 自动覆盖输出目录

## 注意事项

1. **首次运行可能较慢**：
   - exe 文件首次启动需要解压临时文件，可能需要几秒钟

2. **杀毒软件可能误报**：
   - PyInstaller 打包的 exe 可能被误报为病毒
   - 这是误报，可以添加信任

3. **文件大小**：
   - 由于包含 Selenium 和浏览器驱动，exe 文件可能较大（100-200MB）
   - 这是正常的

4. **依赖文件**：
   - 如果程序需要读取 `keywords.xlsx` 等文件，确保这些文件在 exe 同目录下
   - 或者修改代码使用绝对路径

## 分发建议

1. **单文件分发**：
   - 直接分发 `dist/Shop工具合集.exe` 即可
   - 用户双击即可运行，无需安装

2. **创建安装包**（可选）：
   - 可以使用 Inno Setup 或 NSIS 创建安装程序
   - 可以添加桌面快捷方式、开始菜单等

3. **使用说明**：
   - 建议创建一个简单的 README.txt 说明如何使用

## 常见问题

### Q: 打包失败怎么办？
A: 
1. 确保所有依赖都已安装：`pip install -r requirements.txt`
2. 检查 Python 版本（建议 3.8+）
3. 查看错误信息，可能需要添加 `--hidden-import` 参数

### Q: exe 文件太大怎么办？
A: 
- 使用 `--onedir` 代替 `--onefile`（会生成文件夹，但文件更小）
- 或者使用 UPX 压缩（需要安装 UPX）

### Q: 运行时缺少模块？
A: 
- 在打包命令中添加 `--hidden-import=模块名`
- 或者使用 spec 文件配置

### Q: 如何添加图标？
A: 
- 准备一个 `.ico` 图标文件
- 在打包命令中添加 `--icon=图标文件.ico`

## 测试打包结果

打包完成后，建议在干净的 Windows 系统上测试：
1. 复制 exe 文件到新电脑
2. 双击运行
3. 测试所有功能是否正常
